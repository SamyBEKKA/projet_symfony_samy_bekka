{% extends 'base.html.twig' %}

{% block title %}Hello products{% endblock %}

{% block header %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
{{ include('nav.html.twig') }}
{% endblock %}
{% block contenent %}


<div class="example-wrapper">


    <button><a href>/produit/{id}</a>
    </button>

    {% for products in produit %}
    <section class="">
    <div class="container">
        <div class="row border text-center">
            <h2>{{produit.nameProduct}}</h2>
            {# <div class="col-lg-6">
                <div class=" mb-4">
                A VENIR
                    <p>{{ produit.dateproduit|date('d/m/Y')}}</p>
                </div>
            </div> #}
            <div class="col-lg-6">
                <div class=" mb-4">
                    <h4>{{produit.Price}}</h4>
                </div>
            </div>
            <div class="col-lg-6">
                <div class=" mb-4">
                    <h4>{{produit.Quantity}}</h4>
                </div>
            </div>
            <div class="col-lg-6">
                <div class=" mb-4">
                    <h4>{{produit.Stock}}</h4>
                </div>
            </div>
            <div class="col-lg-6">
                <div class=" mb-4">
                    <p>{{produit.shortDescription}}</p>
                </div>
            </div>
            <div class="col-lg-12">
                <div class="mb-4">
                    {# <p>{{produit.longDescription}}</p> #}
                    {# <p> {{categorieProduit.name}}</p> #}
                </div>
            </div>
            <a class="btn btn-primary" href="{{path("produit_item", {id: produits.id})}}">
            Lire...
            </a>
        </div>
    </div>
</section>
<br>
    {% endfor %}
</div>
{% endblock %}